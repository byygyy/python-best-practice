#!/usr/bin/python
# coding=utf-8

# tuple item cannt been changed
names = ('robin', 'joe', 'john');
print(names);
# loop a tuple
for name in names:
    print(name);

# but tuple can been changed
names = ('jason','ivy')
print(names)
for name in names:
    print(name);